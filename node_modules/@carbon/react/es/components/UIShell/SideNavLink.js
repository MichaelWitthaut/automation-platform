/**
 * Copyright IBM Corp. 2016, 2026
 *
 * This source code is licensed under the Apache-2.0 license found in the
 * LICENSE file in the root directory of this source tree.
 */

import { usePrefix } from "../../internal/usePrefix.js";
import Link, { LinkPropTypes } from "./Link.js";
import { SideNavContext } from "./SideNavContext.js";
import SideNavIcon from "./SideNavIcon.js";
import SideNavItem from "./SideNavItem.js";
import SideNavLinkText from "./SideNavLinkText.js";
import { forwardRef, useContext } from "react";
import cx from "classnames";
import PropTypes from "prop-types";
import { jsx, jsxs } from "react/jsx-runtime";
//#region src/components/UIShell/SideNavLink.tsx
/**
* Copyright IBM Corp. 2016, 2026
*
* This source code is licensed under the Apache-2.0 license found in the
* LICENSE file in the root directory of this source tree.
*/
const SideNavLink = forwardRef((props, ref) => {
	const { children, className: customClassName, renderIcon: IconElement, isActive, isSideNavExpanded, large = false, tabIndex, ...rest } = props;
	const { isRail, isSideNavExpanded: contextIsSideNavExpanded } = useContext(SideNavContext);
	const currentIsSideNavExpanded = isSideNavExpanded ?? contextIsSideNavExpanded;
	const prefix = usePrefix();
	const className = cx({
		[`${prefix}--side-nav__link`]: true,
		[`${prefix}--side-nav__link--current`]: isActive,
		[customClassName]: !!customClassName
	});
	return /* @__PURE__ */ jsx(SideNavItem, {
		large,
		children: /* @__PURE__ */ jsxs(Link, {
			...rest,
			className,
			ref,
			tabIndex: tabIndex === void 0 ? !currentIsSideNavExpanded && !isRail ? -1 : 0 : tabIndex,
			children: [IconElement && /* @__PURE__ */ jsx(SideNavIcon, {
				small: true,
				children: /* @__PURE__ */ jsx(IconElement, {})
			}), /* @__PURE__ */ jsx(SideNavLinkText, { children })]
		})
	});
});
SideNavLink.displayName = "SideNavLink";
SideNavLink.propTypes = {
	...LinkPropTypes,
	/**
	* Specify the text content for the link
	*/
	children: PropTypes.node.isRequired,
	/**
	* Provide an optional class to be applied to the containing node
	*/
	className: PropTypes.string,
	/**
	* Specify whether the link is the current page
	*/
	isActive: PropTypes.bool,
	/**
	* Property to indicate if the side nav container is open (or not). Use to
	* keep local state and styling in step with the SideNav expansion state.
	*/
	isSideNavExpanded: PropTypes.bool,
	/**
	* Specify if this is a large variation of the SideNavLink
	*/
	large: PropTypes.bool,
	/**
	* A component used to render an icon.
	*/
	renderIcon: PropTypes.oneOfType([PropTypes.func, PropTypes.object]),
	/**
	* Optional prop to specify the tabIndex of the button. If undefined, it will be applied default validation
	*/
	tabIndex: PropTypes.number
};
//#endregion
export { SideNavLink as default };
