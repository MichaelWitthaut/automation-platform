/**
 * Copyright IBM Corp. 2016, 2026
 *
 * This source code is licensed under the Apache-2.0 license found in the
 * LICENSE file in the root directory of this source tree.
 */

import { usePrefix } from "../../internal/usePrefix.js";
import { Text } from "../Text/Text.js";
import { useId } from "../../internal/useId.js";
import { deprecate } from "../../prop-types/deprecate.js";
import { isComponentElement } from "../../internal/utils.js";
import { useNoInteractiveChildren } from "../../internal/useNoInteractiveChildren.js";
import { AILabel } from "../AILabel/index.js";
import { mergeRefs } from "../../tools/mergeRefs.js";
import { useNormalizedInputProps } from "../../internal/useNormalizedInputProps.js";
import React, { cloneElement, useRef } from "react";
import cx from "classnames";
import PropTypes from "prop-types";
import { jsx, jsxs } from "react/jsx-runtime";
//#region src/components/RadioButton/RadioButton.tsx
/**
* Copyright IBM Corp. 2016, 2026
*
* This source code is licensed under the Apache-2.0 license found in the
* LICENSE file in the root directory of this source tree.
*/
const RadioButton = React.forwardRef((props, ref) => {
	const { className, decorator, disabled = false, hideLabel, id, labelPosition = "right", labelText = "", name, onChange = () => {}, value = "", slug, required, invalid = false, invalidText, warn = false, warnText, readOnly, ...rest } = props;
	const prefix = usePrefix();
	const uid = useId("radio-button");
	const uniqueId = id || uid;
	const normalizedProps = useNormalizedInputProps({
		id: uniqueId,
		readOnly,
		disabled,
		invalid,
		invalidText,
		warn,
		warnText
	});
	function handleOnChange(event) {
		onChange(value, name, event);
	}
	const innerLabelClasses = cx(`${prefix}--radio-button__label-text`, { [`${prefix}--visually-hidden`]: hideLabel });
	const wrapperClasses = cx(className, `${prefix}--radio-button-wrapper`, {
		[`${prefix}--radio-button-wrapper--label-${labelPosition}`]: labelPosition !== "right",
		[`${prefix}--radio-button-wrapper--slug`]: slug,
		[`${prefix}--radio-button-wrapper--decorator`]: decorator,
		[`${prefix}--radio-button-wrapper--invalid`]: normalizedProps.invalid,
		[`${prefix}--radio-button-wrapper--warning`]: normalizedProps.warn
	});
	const inputRef = useRef(null);
	const labelRef = useRef(null);
	const candidate = slug ?? decorator;
	const normalizedDecorator = isComponentElement(candidate, AILabel) ? cloneElement(candidate, { size: candidate.props?.["kind"] === "inline" ? "md" : "mini" }) : candidate;
	useNoInteractiveChildren(labelRef, "The RadioButton component `labelText` prop must have no interactive content");
	return /* @__PURE__ */ jsxs("div", {
		className: wrapperClasses,
		children: [
			/* @__PURE__ */ jsx("input", {
				...rest,
				type: "radio",
				className: `${prefix}--radio-button`,
				onChange: handleOnChange,
				id: uniqueId,
				ref: mergeRefs(inputRef, ref),
				disabled: normalizedProps.disabled,
				value,
				name,
				required,
				readOnly
			}),
			/* @__PURE__ */ jsxs("label", {
				htmlFor: uniqueId,
				className: `${prefix}--radio-button__label`,
				ref: labelRef,
				children: [/* @__PURE__ */ jsx("span", { className: `${prefix}--radio-button__appearance` }), labelText && /* @__PURE__ */ jsx(Text, {
					className: innerLabelClasses,
					children: labelText
				})]
			}),
			labelText && slug ? normalizedDecorator : labelText && decorator ? /* @__PURE__ */ jsx("div", {
				className: `${prefix}--radio-button-wrapper-inner--decorator`,
				children: normalizedDecorator
			}) : null,
			normalizedProps.validation
		]
	});
});
RadioButton.displayName = "RadioButton";
RadioButton.propTypes = {
	/**
	* Specify whether the `<RadioButton>` is currently checked
	*/
	checked: PropTypes.bool,
	/**
	* Provide an optional className to be applied to the containing node
	*/
	className: PropTypes.string,
	/**
	* **Experimental**: Provide a decorator component to be rendered inside the `RadioButton` component
	*/
	decorator: PropTypes.node,
	/**
	* Specify whether the `<RadioButton>` should be checked by default
	*/
	defaultChecked: PropTypes.bool,
	/**
	* Specify whether the control is disabled
	*/
	disabled: PropTypes.bool,
	/**
	* Specify whether the label should be hidden, or not
	*/
	hideLabel: PropTypes.bool,
	/**
	* Provide a unique id for the underlying `<input>` node
	*/
	id: PropTypes.string,
	/**
	* Provide where label text should be placed
	* NOTE: `top`/`bottom` are deprecated
	*/
	labelPosition: PropTypes.oneOf(["right", "left"]),
	/**
	* Provide label text to be read by screen readers when interacting with the
	* control
	*/
	labelText: PropTypes.node.isRequired,
	/**
	* Provide a name for the underlying `<input>` node
	*/
	name: PropTypes.string,
	/**
	* Provide an optional `onChange` hook that is called each time the value of
	* the underlying `<input>` changes
	*/
	onChange: PropTypes.func,
	/**
	* Provide a handler that is invoked when a user clicks on the control
	*/
	onClick: PropTypes.func,
	/**
	* `true` to specify if the control is required.
	*/
	required: PropTypes.bool,
	/**
	* Specify whether the control is currently invalid
	*/
	invalid: PropTypes.bool,
	/**
	* Provide the text that is displayed when the control is in an invalid state
	*/
	invalidText: PropTypes.node,
	/**
	* Specify whether the control is currently in warning state
	*/
	warn: PropTypes.bool,
	/**
	* Provide the text that is displayed when the control is in warning state
	*/
	warnText: PropTypes.node,
	/**
	* Specify whether the RadioButton should be read-only
	*/
	readOnly: PropTypes.bool,
	/**
	* **Experimental**: Provide a `Slug` component to be rendered inside the `RadioButton` component
	*/
	slug: deprecate(PropTypes.node, "The `slug` prop has been deprecated and will be removed in the next major version. Use the decorator prop instead."),
	/**
	* Specify the value of the `<RadioButton>`
	*/
	value: PropTypes.oneOfType([PropTypes.string, PropTypes.number])
};
//#endregion
export { RadioButton as default };
