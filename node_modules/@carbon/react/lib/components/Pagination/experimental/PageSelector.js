/**
 * Copyright IBM Corp. 2016, 2026
 *
 * This source code is licensed under the Apache-2.0 license found in the
 * LICENSE file in the root directory of this source tree.
 */

const require_runtime = require("../../../_virtual/_rolldown/runtime.js");
const require_usePrefix = require("../../../internal/usePrefix.js");
const require_useId = require("../../../internal/useId.js");
const require_index = require("../../Select/index.js");
const require_index$1 = require("../../SelectItem/index.js");
let react = require("react");
react = require_runtime.__toESM(react);
let classnames = require("classnames");
classnames = require_runtime.__toESM(classnames);
let prop_types = require("prop-types");
prop_types = require_runtime.__toESM(prop_types);
let react_jsx_runtime = require("react/jsx-runtime");
//#region src/components/Pagination/experimental/PageSelector.js
/**
* Copyright IBM Corp. 2016, 2023
*
* This source code is licensed under the Apache-2.0 license found in the
* LICENSE file in the root directory of this source tree.
*/
/**
* @deprecated Use `Pagination` with the `renderPageSelect` prop instead.
* `unstable_PageSelector` / `preview_PageSelector` will be removed in v12.
*/
function PageSelector({ className = null, currentPage, id = 1, labelText = "Current page number", totalPages, ...other }) {
	const namespace = `${require_usePrefix.usePrefix()}--unstable-pagination__page-selector`;
	const instanceId = `${namespace}__select-${require_useId.useId()}`;
	(0, react.useEffect)(() => {
		if (process.env.NODE_ENV !== "production") console.warn("[Carbon] `unstable_PageSelector` / `preview_PageSelector` is deprecated and will be removed in v12. Use `Pagination` with the `renderPageSelect` prop instead.");
	}, []);
	const renderPages = (total) => {
		const pages = [];
		for (let counter = 1; counter <= total; counter += 1) pages.push(/* @__PURE__ */ (0, react_jsx_runtime.jsx)(require_index$1.default, {
			value: counter,
			text: String(counter)
		}, counter));
		return pages;
	};
	return /* @__PURE__ */ (0, react_jsx_runtime.jsx)(require_index.default, {
		className: (0, classnames.default)(namespace, className),
		hideLabel: true,
		id: instanceId || id,
		inline: true,
		labelText,
		value: currentPage,
		...other,
		children: renderPages(totalPages)
	});
}
PageSelector.propTypes = {
	/** Extra class names to add. */
	className: prop_types.default.string,
	/** The current page. */
	currentPage: prop_types.default.number.isRequired,
	/** The unique ID of this component instance. */
	id: prop_types.default.oneOfType([prop_types.default.string, prop_types.default.number]),
	/** Translatable string to label the page selector element. */
	labelText: prop_types.default.string,
	/**
	* Total number of pages.
	* This value is calculated using a valid `totalItems` prop passed to the parent `Unstable_Pagination`.
	*/
	totalPages: prop_types.default.number.isRequired
};
//#endregion
exports.default = PageSelector;
